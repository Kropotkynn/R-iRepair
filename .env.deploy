# =====================================================
# R iRepair - Configuration Deploy (Alias de Production)
# =====================================================
# Ce fichier est un alias pour .env.production
# Utilisé par certains scripts de déploiement

# =====================================================
# 🔐 SÉCURITÉ - CHANGEZ CES VALEURS OBLIGATOIREMENT !
# =====================================================

# Base de données PostgreSQL
DB_HOST=postgres
DB_PORT=5432
DB_USER=rirepair_user
DB_PASSWORD=CHANGEZ_CE_MOT_DE_PASSE_SECURISE_123!
DB_NAME=rirepair
DB_SSL=false

# JWT Secret (minimum 32 caractères)
# Générez avec: openssl rand -base64 32
JWT_SECRET=CHANGEZ_CETTE_CLE_JWT_MINIMUM_32_CARACTERES_SUPER_SECURE

# Redis
REDIS_URL=redis://rirepair-redis:6379
REDIS_PASSWORD=CHANGEZ_MOT_DE_PASSE_REDIS_SECURISE

# =====================================================
# 🌐 DOMAINE ET URLs
# =====================================================

# Domaine principal
DOMAIN=votre-domaine.com

# URLs publiques (changez votre-domaine.com)
NEXT_PUBLIC_API_URL=https://votre-domaine.com/api/v1
NEXT_PUBLIC_BASE_URL=https://votre-domaine.com
NEXT_PUBLIC_WS_URL=wss://votre-domaine.com

# URLs internes (ne pas modifier)
FRONTEND_URL=http://frontend:3000
BACKEND_URL=http://backend:8000

# =====================================================
# 📧 CONFIGURATION EMAIL (SMTP)
# =====================================================

# Gmail (recommandé pour débuter)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=votre-email@gmail.com
SMTP_PASSWORD=votre-mot-de-passe-application
SMTP_FROM=noreply@votre-domaine.com
SMTP_FROM_NAME=R iRepair

# =====================================================
# 🚀 ENVIRONNEMENT
# =====================================================

NODE_ENV=production
PORT=8000
FRONTEND_PORT=3000

# =====================================================
# 🔧 CONFIGURATION APPLICATION
# =====================================================

# Informations de l'application
NEXT_PUBLIC_APP_NAME=R iRepair
NEXT_PUBLIC_APP_VERSION=1.0.0

# Origines autorisées (CORS)
ALLOWED_ORIGINS=https://votre-domaine.com,https://www.votre-domaine.com

# =====================================================
# 📊 MONITORING ET LOGS
# =====================================================

# Niveau de logs (debug, info, warn, error)
LOG_LEVEL=info

# Activer les métriques Prometheus
ENABLE_METRICS=true

# Sentry (optionnel - pour le tracking d'erreurs)
SENTRY_DSN=
SENTRY_ENVIRONMENT=production

# =====================================================
# 🎯 FEATURES FLAGS
# =====================================================

# Activer/désactiver des fonctionnalités
NEXT_PUBLIC_ENABLE_LIVE_CHAT=false
NEXT_PUBLIC_ENABLE_NOTIFICATIONS=true
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# =====================================================
# 📈 ANALYTICS (Optionnel)
# =====================================================

# Google Analytics
NEXT_PUBLIC_GA_ID=

# Facebook Pixel
NEXT_PUBLIC_FB_PIXEL_ID=

# =====================================================
# 💳 PAIEMENT (Optionnel - pour future intégration)
# =====================================================

# Stripe
STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# PayPal
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=

# =====================================================
# 🔔 NOTIFICATIONS (Optionnel)
# =====================================================

# Twilio (SMS)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Firebase Cloud Messaging (Push notifications)
FCM_SERVER_KEY=

# =====================================================
# 🗄️ STOCKAGE (Optionnel)
# =====================================================

# AWS S3 (pour stocker les fichiers)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=eu-west-3
AWS_S3_BUCKET=

# =====================================================
# 🔒 SÉCURITÉ AVANCÉE
# =====================================================

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Session
SESSION_SECRET=CHANGEZ_CE_SECRET_DE_SESSION_MINIMUM_32_CARACTERES
SESSION_MAX_AGE=86400000

# CORS
CORS_ENABLED=true

# =====================================================
# 🐳 DOCKER (Ne pas modifier sauf besoin spécifique)
# =====================================================

COMPOSE_PROJECT_NAME=rirepair
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# =====================================================
# 📝 NOTES
# =====================================================
# 1. Ne JAMAIS commiter ce fichier avec des vraies valeurs
# 2. Utilisez des mots de passe forts (minimum 16 caractères)
# 3. Changez TOUS les secrets par défaut
# 4. Pour Gmail, utilisez un "mot de passe d'application"
# 5. Sauvegardez ce fichier en lieu sûr après configuration
