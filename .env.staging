# =====================================================
# R iRepair - Configuration Staging
# =====================================================

# =====================================================
# 🔐 SÉCURITÉ
# =====================================================

# Base de données PostgreSQL
DB_HOST=postgres
DB_PORT=5432
DB_USER=rirepair_user
DB_PASSWORD=CHANGEZ_CE_MOT_DE_PASSE_STAGING
DB_NAME=rirepair_staging
DB_SSL=false

# JWT Secret
JWT_SECRET=CHANGEZ_CETTE_CLE_JWT_STAGING

# Redis
REDIS_URL=redis://rirepair-redis:6379
REDIS_PASSWORD=CHANGEZ_MOT_DE_PASSE_REDIS_STAGING

# =====================================================
# 🌐 DOMAINE ET URLs
# =====================================================

# Domaine staging
DOMAIN=staging.votre-domaine.com

# URLs publiques
NEXT_PUBLIC_API_URL=https://staging.votre-domaine.com/api/v1
NEXT_PUBLIC_BASE_URL=https://staging.votre-domaine.com
NEXT_PUBLIC_WS_URL=wss://staging.votre-domaine.com

# URLs internes
FRONTEND_URL=http://frontend:3000
BACKEND_URL=http://backend:8000

# =====================================================
# 📧 CONFIGURATION EMAIL
# =====================================================

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=votre-email@gmail.com
SMTP_PASSWORD=votre-mot-de-passe-application
SMTP_FROM=staging@votre-domaine.com
SMTP_FROM_NAME=R iRepair Staging

# =====================================================
# 🚀 ENVIRONNEMENT
# =====================================================

NODE_ENV=staging
PORT=8000
FRONTEND_PORT=3000

# =====================================================
# 🔧 CONFIGURATION APPLICATION
# =====================================================

NEXT_PUBLIC_APP_NAME=R iRepair Staging
NEXT_PUBLIC_APP_VERSION=1.0.0-staging

ALLOWED_ORIGINS=https://staging.votre-domaine.com

# =====================================================
# 📊 MONITORING ET LOGS
# =====================================================

LOG_LEVEL=debug
ENABLE_METRICS=true

# =====================================================
# 🎯 FEATURES FLAGS
# =====================================================

NEXT_PUBLIC_ENABLE_LIVE_CHAT=false
NEXT_PUBLIC_ENABLE_NOTIFICATIONS=true
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# =====================================================
# 🔒 SÉCURITÉ AVANCÉE
# =====================================================

RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

SESSION_SECRET=CHANGEZ_CE_SECRET_DE_SESSION_STAGING
SESSION_MAX_AGE=86400000

CORS_ENABLED=true

# =====================================================
# 🐳 DOCKER
# =====================================================

COMPOSE_PROJECT_NAME=rirepair-staging
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1
